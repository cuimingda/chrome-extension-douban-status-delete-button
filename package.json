{
  "name": "chrome-extension-douban-status-delete-button",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that adds a hard delete helper to supported Douban status pages.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "jsdom": "^28.1.0"
  }
}
